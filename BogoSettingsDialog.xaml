<Window x:Class="MultiWindowApp.BogoSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка BOGO сортировки" 
        Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Настройка ограничений BOGO сортировки" 
                   FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Максимальное количество итераций:" 
                   Margin="0,0,0,5"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="MaxIterationsTextBox" Text="100000" Width="320" Padding="3"/>

        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Сохранить" Width="80" Background="#FF0078D7" Foreground="White" 
                    FontWeight="SemiBold" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>